# BoanClaw 조직정책 서버 기능

버전: v0.1.0 · 2026.03 · Samsung GenAI Security  
참조: Antigravity antigravity-unleash.goog + Claude.app 조직관리

---

## 1. 개요

### 역할

- 조직 단위로 BoanClaw 에이전트의 동작 정책을 중앙 배포한다.
- 에이전트 기동 시 체크인 → 정책 응답 → 미응답 시 기동 차단(설계 원칙).
- 관리자 콘솔 UI를 제공한다(Claude 앱 조직 설정과 유사한 UX 목표).

### 엔드포인트(목표 운영 URL)

- 정책·에이전트 API 베이스: `https://policy.boanclaw.io/org/{org_id}/v1/`

### 관리자 콘솔

- `https://console.boanclaw.io/`

---

## 2. 에이전트 체크인 프로토콜

### 기동 시 흐름(설계)

```
BoanClaw 기동
    │
    ▼
POST /org/{org_id}/v1/checkin
    │
    ├─► 200 OK → status, policy, policy_sig 수신 → 서명 검증 → 기동 계속
    │
    └─► 403 Forbidden → 차단 사유 표시 → 기동 중단(또는 업데이트 유도)
```

### 요청 JSON

에이전트는 기동 시 아래 형식으로 체크인한다. `sig`는 `device_secret`으로 페이로드에 대한 HMAC-SHA256 등으로 계산한다(필드 정의는 구현 시 명세 확정).

```json
{
  "instance_id": "uuid-...",
  "org_id": "sds-corp",
  "boanclaw_version": "1.3.0",
  "os": "linux",
  "hostname": "dev-machine-01",
  "user_id": "user@sds.com",
  "timestamp": "2026-03-19T00:00:00Z",
  "sig": "hmac-sha256(payload, device_secret)"
}
```

### 응답: 200 OK (허용)

```json
{
  "status": "allowed",
  "policy_version": 42,
  "policy": { },
  "policy_sig": "ed25519:abcd...",
  "ttl_seconds": 3600,
  "message": null
}
```

- `status`: `allowed` | `blocked` | `update_required` (문자열; 조직 정책에 따라 확장 가능)
- `policy`: 전체 정책 객체(아래 스키마와 동일 계열)
- `policy_sig`: 벤더 비밀키로 서명한 값(에이전트는 내장 공개키로 검증)
- `ttl_seconds`: 다음 체크인 전까지 로컬 캐시로 인정할 수 있는 시간(초)

### 응답: 403 Forbidden (차단)

```json
{
  "status": "blocked",
  "reason": "version_not_allowed",
  "message": "BoanClaw 1.3.0은 조직 정책에서 차단됨. 1.3.3 이상으로 업데이트 필요.",
  "min_version": "1.3.3"
}
```

### 정책 서명 검증 절차

1. 에이전트 바이너리에 **벤더 공개키(ed25519)** 를 내장한다.
2. 서버가 내려준 정책 본문(또는 정책의 정규 직렬화 바이트)과 `policy_sig`를 받는다.
3. 공개키로 서명을 검증한다.
4. 검증 실패 시 **기동·적용을 중단**한다(fail-closed).

---

## 3. 정책 스키마 (Policy Schema)

아래는 조직 정책 문서의 **완전한 예시**이다. 필드 의미는 주석으로 표기한다(문서용; 런타임에는 주석 없는 JSON 사용).

```jsonc
{
  // 조직 식별자
  "org_id": "sds-corp",
  // 정책 버전(단조 증가 권장)
  "policy_version": 42,
  // 발급 시각(UTC ISO8601)
  "issued_at": "2026-03-19T00:00:00Z",
  // 정책 만료 시각(만료 후 에이전트 동작은 조직 정책에 따름)
  "expires_at": "2026-06-19T00:00:00Z",

  // 에이전트 최소 버전, 차단 버전 목록, 강제 업데이트 기한·채널
  "version_policy": {
    "min_version": "1.2.0",
    "blocked_versions": ["1.3.1", "1.3.2"],
    "update_deadline": "2026-06-01",
    "update_channel": "stable"
  },

  // 허용 모델 ID, 기본 모델, 추론 전용 모델 차단 여부
  "model_policy": {
    "allowed_models": [
      "claude-3-5-sonnet",
      "nvidia/nemotron-3-super-120b-a12b",
      "gpt-4o"
    ],
    "default_model": "claude-3-5-sonnet",
    "block_reasoning_models": false
  },

  // egress 모드, 허용 엔드포인트, TLS 검사, 실패 시 폐쇄 여부
  "network_policy": {
    "mode": "allowlist",
    "allowed_endpoints": [
      { "host": "api.anthropic.com", "port": 443, "methods": ["POST"] },
      { "host": "integrate.api.nvidia.com", "port": 443, "methods": ["POST"] },
      { "host": "slack.com", "port": 443, "access": "full" },
      { "host": "api.slack.com", "port": 443, "access": "full" },
      { "host": "nim.sds-internal.corp", "port": 8000, "access": "full" }
    ],
    "tls_inspect": true,
    "fail_closed": true
  },

  // 협업·메시징 채널별 허용 범위
  "channel_policy": {
    "slack": {
      "enabled": true,
      "group_policy": "allowlist",
      "allowed_workspaces": ["T0123ABC"],
      "allowed_channels": ["C0123DEF", "C0456GHI"]
    },
    "teams": { "enabled": false },
    "whatsapp": { "enabled": false },
    "cli": { "enabled": true }
  },

  // 사용자·그룹별 권한 단계(level)
  "rbac_policy": {
    "default_level": 1,
    "users": {
      "admin@sds.com": { "level": 3 },
      "dev@sds.com": { "level": 2 },
      "readonly@sds.com": { "level": 0 }
    },
    "groups": {
      "sds-devops": { "level": 2 },
      "sds-readonly": { "level": 0 }
    }
  },

  // 사내(inner) 제공자와 외부(outer) 제공자 목록
  "provider_policy": {
    "inner_provider": {
      "enabled": true,
      "endpoint": "https://nim.sds-internal.corp/v1",
      "dlp_enabled": true,
      "sensitive_data_route": "inner_only"
    },
    "outer_providers": ["anthropic", "nvidia", "openai"]
  },

  // 감사 로그 수집·보내기·암호화 설정
  "audit_policy": {
    "enabled": true,
    "log_prompts": true,
    "log_responses": true,
    "log_tool_calls": true,
    "export_endpoint": "s3://sds-audit-logs/boanclaw/",
    "export_format": "jsonl",
    "encrypt": true,
    "encryption_key_ref": "azure-kv://sds-vault/boanclaw-log-key"
  },

  // 기능 플래그(원격 토글)
  "feature_flags": {
    "remote_control": true,
    "agent_swarms": false,
    "scheduled_tasks": true,
    "web_access": false
  }
}
```

---

## 4. 관리자 콘솔 UI

### 조직설정 탭

조직 단위 정책(버전·모델·네트워크·채널·RBAC)을 폼으로 편집하고 저장한다.

```
┌─────────────────────────────────────────────────────────────┐
│  조직: SDS Corporation                     [정책 저장]      │
├─────────────────────────────────────────────────────────────┤
│  버전 정책                                                   │
│   허용 최소 버전: [1.2.0    ]                                │
│   차단 버전:     [1.3.1, 1.3.2]  [+ 추가]                   │
│   업데이트 기한: [2026-06-01]                                │
├─────────────────────────────────────────────────────────────┤
│  모델 허용 목록                                              │
│   [✓] claude-3-5-sonnet                                     │
│   [✓] nvidia/nemotron-3-super-120b-a12b                     │
│   [ ] gpt-4o                                                 │
├─────────────────────────────────────────────────────────────┤
│  네트워크 정책                                               │
│   모드: [● allowlist ○ denylist]                            │
│   허용 엔드포인트: [표 형태 편집]                            │
├─────────────────────────────────────────────────────────────┤
│  채널 설정                                                   │
│   Slack:    [✓ 활성] 허용 채널: [#dev, #infra]              │
│   Teams:    [ 비활성]                                        │
│   WhatsApp: [ 비활성]                                        │
├─────────────────────────────────────────────────────────────┤
│  사용자 권한 (RBAC)                                          │
│   [표: user_id | level | 적용일]                            │
└─────────────────────────────────────────────────────────────┘
```

### 감사로그 탭

기간·이벤트 유형 필터로 감사 이벤트를 조회한다.

```
┌─────────────────────────────────────────────────────────────┐
│  기간: [2026-03-01] ~ [2026-03-19]     [조회]              │
├─────────────────────────────────────────────────────────────┤
│  이벤트 유형: [■ llm_request ■ tool_call ■ policy_block]   │
├─────────────────────────────────────────────────────────────┤
│  timestamp          user            event_type   status     │
│  2026-03-19 11:00  user@sds.com    llm_request  success    │
│  2026-03-19 11:01  user@sds.com    tool_call    success    │
│  2026-03-19 11:02  other@sds.com   policy_block blocked    │
└─────────────────────────────────────────────────────────────┘
```

### 라이선스 탭

- 라이선스 키
- 유효 기간
- 활성 인스턴스 수 / 허용 인스턴스 수
- CSP 연동 상태(Vault 연결 여부 등)

---

## 5. API 목록

### 에이전트용 API (인증: device_secret HMAC)

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `POST` | `/org/{org_id}/v1/checkin` | 기동 체크인 및 정책 수신 |
| `GET` | `/org/{org_id}/v1/policy` | 정책 단독 조회(캐시 갱신용) |
| `POST` | `/org/{org_id}/v1/heartbeat` | 주기적 생존 신고(예: 30분 간격) |
| `POST` | `/org/{org_id}/v1/event` | 이벤트 리포트(정책 위반 등) |

### 관리자용 API (인증: OAuth2 / SAML SSO)

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET` | `/admin/orgs/{org_id}/policy` | 현재 정책 조회 |
| `PUT` | `/admin/orgs/{org_id}/policy` | 정책 업데이트 |
| `GET` | `/admin/orgs/{org_id}/instances` | 활성 인스턴스 목록 |
| `DELETE` | `/admin/orgs/{org_id}/instances/{id}` | 특정 인스턴스 강제 차단 |
| `GET` | `/admin/orgs/{org_id}/audit-logs` | 감사로그 조회 |
| `POST` | `/admin/orgs/{org_id}/policy/publish` | 정책 배포(서명 포함) |

---

## 6. 인프라 구성

### 스택(데이터 플로우)

```
[CDN / DDoS 보호 (CloudFront / Cloudflare)]
        ↓
[API Gateway (Kong / AWS API Gateway)]
        ↓
[Policy Server (Go/Rust 권장)]
  - 정책 CRUD
  - HMAC 검증(에이전트 요청)
  - 정책 서명(ed25519, 비밀키는 HSM 보관 권장)
        ↓
[DB: PostgreSQL]
  - 조직, 정책, 인스턴스, 이벤트
```

### 가용성·오프라인

- **다중 리전 배포:** 에이전트 체크인 실패 시 fail-closed 설계와 맞물려 **고가용성(HA)이 필수**에 가깝다.
- **오프라인 모드:** 마지막으로 수신·검증한 정책을 캐시해 **최대 24시간** 등 한도 내에서 동작할 수 있게 한다(조직 설정으로 `allow_offline: true/false` 등 제어 가능하도록 설계).

---

## 7. 현재 구현 상태 (boan-policy-server)

구현 위치: `SDSclawBLUE/BoanClaw/src/packages/boan-policy-server/`

### 구현 계획서(06_구현계획)와의 대응 요약

- **문서 03 설계 반영:** ed25519 서명 기반 정책 배포, Credential TTL·자산 헌법(boan-asset-constitution) 등과 함께 **S4 CSP 인프라**의 Terraform 모듈로 올릴 대상으로 정의되어 있다.
- **Phase 3 (3-1) 계획:** `GET /org/{org_id}/policy.json` 형태의 REST, ed25519 서명, DLP·Network Whitelist·RBAC·feature flags 항목, **최대 10버전 보존 및 rollback API** 를 목표로 명시되어 있다.
- **배포:** `deploy/terraform/modules/policy-server` 등으로 Cloud Run / ECS 등에 배포하는 그림이 계획서에 포함된다.
- **관리 UI:** `boan-admin-console`의 PolicyManager(저장 시 자동 ed25519 서명·배포, 버전 이력·롤백)와 API 클라이언트로 연동하는 계획이다.

### 코드 기준으로 **현재 동작하는 것**

- **HTTP 서버:** `net/http` 기반. listen 주소는 환경변수 `BOAN_LISTEN`(미설정 시 **`:8080`**). Docker 이미지는 `8080` 노출.
- **헬스체크:** `GET /healthz` → 본문 `ok`.
- **공개키 조회:** `GET /pubkey` → JSON `{"pub": "<ed25519 public key bytes>"}` (에이전트·운영 도구가 검증에 사용 가능).
- **정책 조회(서명 포함):** `GET /org/{org_id}/policy.json` — 저장소에서 최신 정책을 읽은 뒤 `internal/signing`으로 **ed25519 서명**하여 `signature` 필드에 실어 JSON으로 반환.
- **네트워크 정책(별도 서명 페이로드):** `GET /org/{org_id}/network-policy.json` — `endpoints`·`updated_at`만 담은 문서를 직렬화한 바이트에 대해 서명하고, `policy` + `signature` 형태로 반환.
- **정책 갱신(관리용 경로):** `POST /org/{org_id}/policy` — 본문 JSON을 디코드해 버전 증가 후 저장(**현 단계에서 OAuth2/SAML 등 관리자 인증은 없음**).
- **버전 목록:** `GET /org/{org_id}/policy/versions`.
- **롤백:** `POST /org/{org_id}/policy/rollback/{version}` — 지정 버전을 불러와 새 버전으로 다시 저장.
- **저장소:** **PostgreSQL 없음**. `BOAN_DATA_DIR`(기본 `/data/policies`) 하위에 조직별 `v{N}.json` 파일로 보관. **최대 10개** 버전 유지(`MaxVersions`).
- **키:** `BOAN_KEY_DIR`(기본 `/etc/boan-policy`)에 `ed25519.priv` / `ed25519.pub`를 두거나 없으면 생성(`crypto/ed25519`).

### 현재 `Policy` 구조(구현체; 설계 스키마의 부분 집합)

실제 Go 타입은 `version`, `org_id`, `updated_at`, `network_whitelist`, `dlp_rules`, `rbac`, `allow_models`, `features`, `signature` 등에 맞춰 있으며, 본 문서 **§3 전체 JSON 스키마**와는 필드명·구조가 완전히 일치하지 않는다(향후 정합화 필요).

### 아직 없거나 스텁 수준인 것

- **`/v1/checkin`, `heartbeat`, `event`** 및 설계상 **device_secret HMAC** 검증.
- **관리자 REST** (`/admin/orgs/...`) 및 **OAuth2/SAML SSO**.
- **CDN·API Gateway** 앞단.
- **PostgreSQL** 및 인스턴스·감사로그 테이블 연동.

---

## 8. 향후 확장 계획

운영 환경을 위해 다음을 단계적으로 추가한다.

- **PostgreSQL(또는 동등한 관계형 DB)** 백엔드: 조직·정책 버전·인스턴스·이벤트·감사 메타데이터의 영속화와 트랜잭션.
- **CDN / DDoS 보호 계층:** 전 세계 에이전트의 체크인·정책 pull에 대한 지연·가용성·남용 방어.
- **다중 리전 HA:** 활성-활성 또는 활성-대기, 헬스체크·트래픽 페일오버.
- **관리자 콘솔 SAML SSO:** 조직 IdP와 연동한 관리 API 보호.
- **device_secret HMAC 검증:** 에이전트 API 전 구간에 요청 무결성·디바이스 바인딩 적용.
- **설계 URL과 구현 경로 정렬:** `/v1/` 네임스페이스, `checkin` 응답의 `policy_version`·`ttl_seconds` 등 설계 필드와의 **완전한 스키마 일치**.

---

*본 문서는 `docs/03_조직정책서버_설계.txt` 및 `docs/06_구현계획.txt`의 정책 서버 관련 내용을 통합하고, `src/packages/boan-policy-server` 소스를 반영하여 작성하였다.*
