apiVersion: apps/v1
kind: Deployment
metadata:
  name: boan-audit-agent
  namespace: boan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boan-audit-agent
  template:
    metadata:
      labels:
        app: boan-audit-agent
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: boan-audit-agent
          image: boanclaw/boan-audit-agent:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "256Mi"
              cpu: "250m"
            requests:
              memory: "64Mi"
              cpu: "50m"
          env:
            - name: BOAN_ORG_ID
              valueFrom:
                secretKeyRef:
                  name: boan-org-config
                  key: org_id
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 30
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: audit-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: boan-audit-agent
  namespace: boan
spec:
  selector:
    app: boan-audit-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
