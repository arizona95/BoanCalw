apiVersion: apps/v1
kind: Deployment
metadata:
  name: boan-llm-registry
  namespace: boan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boan-llm-registry
  template:
    metadata:
      labels:
        app: boan-llm-registry
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: boan-llm-registry
          image: boanclaw/boan-llm-registry:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "256Mi"
              cpu: "250m"
            requests:
              memory: "64Mi"
              cpu: "50m"
          env:
            - name: BOAN_ORG_ID
              valueFrom:
                secretKeyRef:
                  name: boan-org-config
                  key: org_id
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 30
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: boan-llm-registry
  namespace: boan
spec:
  selector:
    app: boan-llm-registry
  ports:
    - name: http
      port: 8080
      targetPort: 8080
